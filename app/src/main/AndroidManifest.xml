<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="study.sang.androidbasestudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DrawCricleActivity" />
        <activity android:name=".activity.ClockActivity" />
        <activity android:name=".activity.SeekBarActivity" />
        <activity android:name=".activity.ViewSwitchActivity" />
        <activity android:name=".activity.TextSwitchActivity" />
        <activity android:name=".activity.ToastActivity"></activity>
    </application>

</manifest>